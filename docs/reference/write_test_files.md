# Writes example input files to a folder named "test_input", within the working directory.

Writes example input files to a folder named "test_input", within the
working directory.

## Usage

``` r
write_test_files()
```
